<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="initial-scale=1, maximum-scale=1">
    <meta name="viewport" content="width=device-width">
    <title><%= title %></title>
    <!-- CSS Files -->
    <link href="/css/reset.css" rel="stylesheet" />
    <link href="/css/style.css" rel="stylesheet" />
    <link href="/css/slider.css" rel="stylesheet" />
    <link href="/css/camera/css/camera.css" rel="stylesheet" />
    <link href="/css/Content/foundation/foundation.css" rel="stylesheet" />
    <link href="/css/Content/foundation/normalize.css" rel="stylesheet" />
    <link href="/css/Content/fonts/ligature.css" rel="stylesheet" />
    <script src="/Scripts/vendor/redirection-mobile.js"></script>
     <script src="/Scripts/app.js"></script>

    <script>
        var currentURL = document.URL
        var n = currentURL.search("index.html");
        var m = currentURL.search("//");
        var newcurrentURL = currentURL.substring(m, n);
        SA.redirection_mobile({
            mobile_url: newcurrentURL + "Mobile/index.html",
            mobile_prefix: "http"
        });
    </script>
 <script src="https://maps.googleapis.com/maps/api/js?v=3.exp&sensor=false"></script>
    

</head>
<body>

        <%    if(title == "Contact me")  {%>
    <%    include partials/contactmehead.ejs %>
    <%    }  %> 

    <% include partials/headernologo.ejs %>
    
    <%    if(title == "Portfolio")  {%>
    <%    include partials/portfolio.ejs %>
    <%    }  %>  

    <%    if(title == "Projects")  {%>
    <%    include partials/projects.ejs %>
    <%    }  %>     

    <%    if(title == "About Me")  {%>
    <%    include partials/aboutme.ejs %>
    <%    }  %>      
    
    <%    if(title == "Services")  {%>
    <%    include partials/services.ejs %>
    <%    }  %> 

    <%    if(title == "Github")  {%>
    <%    include partials/github.ejs %>
    <%    }  %>      
    
    <%    if(title == "Contact me")  {%>
    <%    include partials/contactme.ejs %>
    <%    }  %> 

    <script>

        var map;
        function initialize() {
            var mapOptions = {
                zoom: 16,
                center: new google.maps.LatLng(43.781389, -79.245417),
                mapTypeId: google.maps.MapTypeId.ROADMAP
            };
            map = new google.maps.Map(document.getElementById('map_canvas'),
                mapOptions);
            marker = new google.maps.Marker({
                position: latLng,
                draggable: true,
                zIndex: 1,
                map: mapOptions,
                optimized: false
            });

            var label = new Label({
                map: mapOptions
            });

            label.bindTo('position', marker);
            label.bindTo('text', marker, 'position');
            label.bindTo('visible', marker);
            label.bindTo('clickable', marker);
            label.bindTo('zIndex', marker);
        }

        google.maps.event.addDomListener(window, 'load', initialize);
    </script>
    
    <% include partials/footer.ejs %>
</body>
</html>